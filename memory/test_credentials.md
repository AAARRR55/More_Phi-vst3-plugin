# Test Credentials

No user accounts or private credentials were created for this C++ licensing implementation.

Runtime activation configuration uses environment variables when available:

- `LICENSE_API_BASE_URL`
- `MOREPHI_PUBLIC_CLIENT_TOKEN`
- `MOREPHI_CLIENT_HEADER` (optional, defaults to `X-MorePhi-Client`)